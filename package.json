{
  "name": "movie-browsing-app",
  "private": true,
  "version": "1.0.0",
  "description": "Full stack movie browsing application with React/Vite frontend and Express/MySQL backend.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
